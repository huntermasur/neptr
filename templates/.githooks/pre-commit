#!/bin/sh
# Auto-generated by NEPTR. Keeps the Claude-Code repo index fresh at commit time.
# Activated via `git config core.hooksPath .githooks`.
#
# Non-blocking by design: if neptr is not installed or indexing fails, the commit
# still proceeds. Nothing here should ever stop you from committing.
if command -v neptr >/dev/null 2>&1; then
  neptr index --quiet || true
  git add .docs/REPO_MAP.md .agents/KNOWLEDGE_MAP.md .agents/CAPABILITIES.md 2>/dev/null || true
fi
exit 0
