# Adoption Plan — restructure {{projectName}} into NEPTR's layout

Created {{date}} by `neptr adopt`. Current phase lives in [STATUS.md](STATUS.md).

## Goal

Fit this project fully into NEPTR's structure without changing behaviour:

- move `src/` code into the canonical role-based layout (`app/`, `modules/`,
  `services/`, `data/`, `integrations/`, `shared/`, `config/`);
- move tests to NEPTR's convention (unit tests co-located, shared
  helpers/fixtures and cross-cutting suites in root `tests/`);
- consolidate scattered documentation into the `.docs/` tree;
- stand up a verified Docker setup for the app and its backing services
  (finishing the DRAFT files `neptr adopt` may have written).

The empty section folders and the `.agents/` + `.docs/` hub have already been
added by `neptr adopt`; the risky parts — moving files, fixing imports and
links, and making the containers actually run — are what this workspace plans
and executes.

For what each section is for, read [../../module-map.md](../../module-map.md).

## Current inventories (auto-generated by `neptr adopt`)

The scans of the existing code, docs, tests, and detected services live in
[NOTES.md](NOTES.md). All suggestions are heuristic — the planning agent must
confirm or correct each one.

## Context

<!-- Planning agent: the current structure, the framework, the build/test commands,
     and anything that makes moves risky (path aliases, absolute imports, barrel
     files, bundler config that references paths, generated code). -->

_Not planned yet — the plan phase fills in everything below._

## Approach

<!-- Planning agent: the migration strategy. Prefer small, verifiable batches
     (e.g. one section at a time, typecheck after each) over one big move. Note how
     imports will be fixed (IDE move-refactor, path alias, find/replace) and how the
     entrypoint (main/index, index.html script src) will keep resolving. -->

## Target mapping — code

<!-- Planning agent: the final, confirmed mapping. One row per existing file/folder. -->

| Current path | Target path | Notes |
| --- | --- | --- |

## Target mapping — tests

<!-- Planning agent: one row per test file/dir; note runner config (include/testMatch)
     that must change when tests move. -->

| Current path | Target path | Notes |
| --- | --- | --- |

## Target mapping — docs

<!-- Planning agent: one row per doc; a doc may be moved, merged into an existing
     .docs/ file, or deliberately left (say why). List inbound links to fix. -->

| Current path | Target | Merge/link notes |
| --- | --- | --- |

## Docker plan

<!-- Planning agent: what it takes to make the Docker setup real. Keep the DRAFT
     files as the starting point unless the project already had Docker files —
     then gap-check those against the detected services instead. -->

- [ ] Verify base image, install steps, build line, and CMD entrypoint
- [ ] Wire every required env var (into compose / `.env.example` — never real values)
- [ ] Account for migrations/seed commands the containers must run
- [ ] Add healthchecks / depends_on conditions where startup order matters
- [ ] `docker compose config` parses clean

## Risks & open questions

<!-- Path aliases to update, config files that hardcode paths (vite.config,
     tsconfig, index.html), runner config that pins test paths, docs with many
     inbound links, services whose connection strings are hardcoded. -->

## Out of scope

<!-- Explicit non-goals: no refactors, no renames beyond relocation, no behaviour
     changes, no dependency upgrades. Code and test moves are mechanical; docs may
     be merged but never rewritten in meaning; Docker files may be edited freely,
     but application code must not change to suit them. -->
