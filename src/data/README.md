# data/

Database, storage, repositories, models, and migrations.

The only section that talks to the persistence layer directly. Everything above goes
through the repositories/models defined here rather than issuing queries itself.

Examples: `models/`, `repositories/`, `migrations/`, storage clients, schema definitions.
